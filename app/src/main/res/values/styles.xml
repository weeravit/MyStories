<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme.Pink" parent="AppTheme.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pink_300</item>
        <item name="colorPrimaryDark">@color/pink_400</item>
        <item name="colorAccent">@color/pink_a200</item>
        <item name="colorHeaderCardView">@color/pink_50</item>
    </style>

    <style name="AppTheme.Green" parent="AppTheme.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/teal_500</item>
        <item name="colorPrimaryDark">@color/teal_600</item>
        <item name="colorAccent">@color/teal_700</item>
        <item name="colorHeaderCardView">@color/teal_50</item>
    </style>

    <style name="AppTheme.Blue" parent="AppTheme.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryDark">@color/blue_600</item>
        <item name="colorAccent">@color/blue_700</item>
        <item name="colorHeaderCardView">@color/blue_50</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppTheme.Pink.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.Pink.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
